; platform dev/production PHP settings

upload_max_filesize = 32M
post_max_size = 64M
max_execution_time = 120
memory_limit = 256M

; Don't expose the PHP version in response headers
expose_php = Off

; Use UTC everywhere â€” convert to local time at the presentation layer
date.timezone = UTC
